shamefully-hoist=true

# Support both glibc (Debian/Ubuntu) and musl (Alpine) for optional dependencies
# Required for Rollup platform-specific binaries in Docker builds
supportedArchitectures.libc=glibc,musl
