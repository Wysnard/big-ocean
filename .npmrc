shamefully-hoist=true

# Support multiple platforms for optional dependencies
# Required for Rollup platform-specific binaries in CI (Linux) and Docker (Alpine)
supportedArchitectures.os=current,linux
supportedArchitectures.cpu=current,x64,arm64
supportedArchitectures.libc=current,glibc,musl
